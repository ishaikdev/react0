import Link from 'next/link';


//This is the index page, it is on the route '/' and is being wrapped by Page component
// Check Page component as it is the parent of this page
const Home = props => (
  <div>
    <p>Hey!</p>
  </div>
);

export default Home;
